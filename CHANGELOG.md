# 修改日志(CHANGELOG)

---

## lhy-new 分支修改(2026-08-17)

> 分支:`lhy-new`(基于 master 创建,在 jxytest 的语义分块/混合检索基础上继续迭代)
> 目的:网页端支持**直接上传新文档并自动向量化入库**,且上传前先展示知识库已有文档

### 1. `app.py` —— 网页版(Streamlit)

| 改进项 | 说明 |
|--------|------|
| 展示已有文档 | 侧边栏新增"📚 知识库文档"区块,启动时读取 `knowledge_base/` 目录,列出所有已有文档 |
| 上传即向量化 | 上传新文档后自动完成:保存文件 → 只加载新文件 → 语义分块 → 增量写入 Chroma 向量库 → 更新 chunks.json,无需再手动运行 `build_database.py`,上传完可直接提问 |
| 重名覆盖 | 同名文档直接**覆盖**旧版本,并同步删除旧向量与旧 chunks.json 条目(按 source 匹配),保证向量库中只有新内容 |
| 上传组件重置 | 上传处理完成后清空 file_uploader 中已选文件(key 追加轮次计数 `upload_round` 强制重建组件) |
| 初始化返回值 | `init_qa_system()` 由返回 `(qa_chain, retriever)` 改为 `(qa_chain, retriever, embeddings, db)`,向上传流程暴露嵌入模型与向量库实例 |

### 2. `build_database.py` —— 建库脚本

| 位置 | 原实现 | 新实现 |
|------|--------|--------|
| `load_documents()` | 固定加载知识库全部文档 | 新增可选参数 `only_files`(只加载指定文件名),供网页上传后的增量入库复用 |

### 3. 验证结果

| 测试 | 结果 |
|------|------|
| 语法检查 | py_compile 全部通过 |
| Streamlit 启动 | 后台启动成功,http://localhost:8501 返回 200 |

### 4. 遗留说明

- 网页端增量入库与 `build_database.py` 全量重建互不冲突:上传走增量,重建走清库,两者最终状态一致
- 覆盖重名文档依赖 Chroma 按 `source` 元数据删除,请勿修改 load_documents 中 source 路径的拼接方式

---

## 此前 jxytest 分支修改

> 分支:`jxytest`(基于 master 创建,原分支 master 未做任何改动)
> 目的:针对 RAG 知识库问答系统做 5 项优化 —— 语义分块、混合检索、多轮对话记忆、Prompt 预设、检索结果相似度排序

---

## 一、依赖变更

| 变更 | 说明 |
|------|------|
| 新增 `rank_bm25==0.2.2` | BM25 关键词检索算法实现,纯 Python,无虚拟环境直接全局安装 |
| 环境说明 | 本机 langchain 已升级为 1.x,原 `langchain.chains/memory/retrievers` 已被移除,改用 `langchain_classic` 包导入 EnsembleRetriever(等效组件) |

## 二、新增文件

### 1. `semantic_splitter.py` —— 语义分块器(自写,无新增依赖)

- **解决的问题**:原固定 256 字符切分会把完整语义从句子中间拦腰截断
- **实现原理**:
  1. 清洗 PDF 抽取文本(去除排版换行符,否则每行都被当成一句话,产生碎片块)
  2. 按中文标点(`。！？；`)切句
  3. 用 text2vec 逐句向量化,计算相邻句子余弦相似度(句子向量只算一次,统计阈值和分块共用)
  4. 相似度低于断点阈值处视为话题转换点,在此断开
  5. 块长上限 500 字符,防止话题过长产生超大块
  6. 相邻块重叠 1 句,缓解话题边界处信息丢失
- **自适应阈值(泛用性)**:断点阈值不写死,而是取全篇相邻句相似度分布的 **P15 分位**(实测《中国文化精神》P15≈0.50),另设绝对下限 0.35 兜底。换任何书籍/语料无需重新校准,也不增加向量化次数
- **效果对比**:
  - 修复前(换行符干扰):18153 个碎片块,中位数仅 5 字
  - 固定阈值 0.50 版:1496 块,中位数 126 字
  - **当前版(P15 自适应 + 重叠 1 句):1465 块,中位数 163 字,均值 205 字,≤20 字的碎块从 316 降至 93,分布更均衡**

### 2. `qa_system.py` —— 问答核心组件(app.py 与 chat.py 共用)

- 集中管理:嵌入模型、LLM、混合检索器、问题改写链、问答链、统一提问入口
- 消除 app.py 与 chat.py 的重复代码

### 3. `vector_db/chunks.json` —— 建库时自动保存的文本块

- 运行时构建 BM25 检索器直接读取,避免每次启动重复切分

### 4. `CHANGELOG.md` —— 本修改日志

## 三、修改文件

### 1. `build_database.py` —— 建库脚本

| 位置 | 原实现 | 新实现 |
|------|--------|--------|
| 文本切分 | RecursiveCharacterTextSplitter 固定 256 字符 + 50 重叠 | 调用 `semantic_splitter.split_documents_semantic` 语义分块 |
| 建库流程 | 加载文档 → 切分 → 嵌入 → 入库 | 先初始化嵌入模型(切分和向量化共用),切分后先清库再写 chunks.json |
| 新增函数 | — | `save_chunks_json()`:把文本块保存为 JSON 供 BM25 复用 |

### 2. `config.py` —— 全局配置

| 位置 | 原实现 | 新实现 |
|------|--------|--------|
| API Key | 硬编码空字符串 | 从环境变量 `DEEPSEEK_API_KEY` 读取,不把密钥写进代码 |
| 分块参数 | `CHUNK_SIZE=256`、`CHUNK_OVERLAP=50` | `SEMANTIC_BREAKPOINT_PERCENTILE=15`(自适应断点分位)、`SEMANTIC_THRESHOLD_FLOOR=0.35`(绝对下限)、`SEMANTIC_MAX_CHUNK=500`(块长上限)、`SEMANTIC_OVERLAP_SENTS=1`(块间重叠句数) |
| 检索配置 | 仅 `RETRIEVAL_TOP_K=15` | 新增 `HYBRID_WEIGHTS=[0.5,0.5]`(向量:BM25 融合权重) |
| Prompt | 单个硬编码模板(写死允许推测) | `PROMPT_TEMPLATES` 3 套预设(严谨/平衡/宽松)+ `DEFAULT_PROMPT_STYLE="平衡"` |
| 多轮对话 | 无 | 新增 `CONDENSE_PROMPT` 问题改写模板 |

### 3. `app.py` —— 网页版(Streamlit)

| 改进项 | 说明 |
|--------|------|
| 混合检索 | 向量检索 + BM25 关键词检索经 EnsembleRetriever(RRF 倒数排名融合)合并,权重 0.5:0.5 |
| 多轮记忆 | 新增问题改写链:结合历史对话把"它/这个"等指代词改写成独立查询后再检索(LLM 改写 + 检索,纯 LCEL 实现) |
| Prompt 切换 | 侧边栏新增"回答风格"下拉框,3 套模板实时切换(严谨:禁止推测 / 平衡:允许推测须标明 / 宽松:灵活回答) |
| 相似度排序 | 参考原文片段按向量距离升序(相似度从高到低)展示,每条附距离分数 |
| 历史管理 | 会话历史存 `st.session_state`,天然按浏览器会话隔离,只保留最近 3 轮 |

### 4. `chat.py` —— 命令行版

| 改进项 | 说明 |
|--------|------|
| 同上 | 同步接入混合检索、问题改写多轮记忆、相似度排序展示(显示前 5 个参考片段及距离分数) |
| Prompt | 使用默认"平衡"风格,历史保留最近 6 轮 |

## 四、向量库重建

- 旧库(固定 256 字符分块)已删除,用语义分块器重建:**1465 个语义块**(P15 自适应阈值 + 重叠 1 句)
- 生成文件:`vector_db/chunks.json`(1465 条) + Chroma 库文件

## 五、建库耗时实测(同一本书、同机、模型已缓存)

| 版本 | 分块方式 | 块数 | 建库耗时 |
|------|----------|------|----------|
| master(原版) | 固定 256 字符 + 50 重叠 | 1404 | 67.3 秒 |
| jxytest(固定阈值 0.50) | 语义分块 | 1496 | 192.4 秒 |
| jxytest(当前版) | 语义分块(P15 自适应 + 重叠) | 1465 | 194.7 秒 |

- 当前版比原版慢约 2.9 倍,多出的时间全部花在逐句向量化(约 5900 句,用于找语义断点)
- 自适应百分位和重叠**不增加任何向量化轮次**,耗时增量实测约 1%,可忽略

## 六、验证结果

| 测试 | 结果 |
|------|------|
| 语法/导入检查 | 全部通过(py_compile + import) |
| 建库 | 1465 块,块长中位数 163 字、均值 205 字,分布合理 |
| 端到端问答 Q1:"张岱年认为中国文化的基本精神是什么?" | 正确回答四要素(刚健有为、和与中、崇德利用、天人协调)并引用原文 |
| 端到端追问 Q2:"它主要体现在哪些方面?" | 问题改写链正确把"它"还原为"中国文化的基本精神",回答正确 |
| 相似度排序 | 参考片段按距离分数升序展示,前 3 条分数 0.5561 / 0.6469 / 0.7183(单调递增) |
| Streamlit 启动 | 后台启动成功,http://localhost:8501 返回 200 |

## 七、遗留说明

1. `vector_db/` 体积较大,提交作业时无需上传,可随时用 `python build_database.py` 重建
2. BM25 检索器依赖 `rank_bm25` 包,部署机器需先 `pip install rank_bm25`
3. 自适应百分位阈值已按语料泛化,换书一般无需改配置;若分块过碎/过大,调 `SEMANTIC_BREAKPOINT_PERCENTILE`(分位越小断点越少、块越大)即可
