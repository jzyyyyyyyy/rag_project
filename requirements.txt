# RAG 知识库问答系统 - 依赖包清单
# 安装命令：pip install -r requirements.txt

# LangChain 核心框架
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0

# LangChain 第三方集成
langchain-openai>=0.1.0          # 调用 DeepSeek（OpenAI兼容接口）
langchain-huggingface>=0.0.1     # 加载本地嵌入模型
langchain-chroma>=0.1.0          # Chroma 向量数据库集成
langchain-text-splitters>=0.2.0  # 文本分割器

# 向量数据库底层
chromadb>=0.4.0

# 嵌入模型运行依赖
sentence-transformers>=2.2.0

# 文档解析
pypdf>=4.0.0                     # PDF 解析
python-docx>=1.0.0               # Word 文档解析

# 网页界面
streamlit>=1.30.0
